## Contribution Rules

- Each algorithm lives in its own folder
- `model.py` must be NumPy-only
- `demo.ipynb` must include:
  - visualization
  - sklearn comparison
- README must document at least one failure mode

PRs that skip derivations will be rejected.
